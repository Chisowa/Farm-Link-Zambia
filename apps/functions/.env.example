# ── Firebase Admin SDK ────────────────────────────────────────────────────────
# NOTE: FIREBASE_PROJECT_ID and GOOGLE_CLOUD_PROJECT are RESERVED by Firebase
# and set automatically by the runtime. Do NOT put them in .env or the deploy
# will fail with "Key starts with a reserved prefix".
# For local dev with a service account key (optional):
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-XXXXX@farmlink-zambia.iam.gserviceaccount.com

# ── Google Cloud / Vertex AI ──────────────────────────────────────────────────
VERTEX_AI_LOCATION=us-central1
VERTEX_AI_MODEL_ID=gemini-2.0-flash-001

# ── GCS Bucket (agricultural document storage) ────────────────────────────────
GCS_BUCKET=famlinkdocs

# ── Runtime ───────────────────────────────────────────────────────────────────
NODE_ENV=development

# ── Firebase Emulators (uncomment for local development) ──────────────────────
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
# FIRESTORE_EMULATOR_HOST=localhost:8080
